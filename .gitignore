# 取得ログ（実行環境ごとに内容が変わるため管理しない）
logs/
# 1時間足のログ（実行環境ごとに内容が変わるため管理しない）
logs_1h/
# データ（実行環境ごとに内容が変わるため管理しない）
data/

# 生成したCSV（実行環境ごとに内容が変わるため管理しない）
csv/
csv_legacy/

# 環境変数ファイル（機密）
.env
.env.local
apps/log_analyzer/.env
apps/log_analyzer/.env.local
